import styled from 'styled-components'

function BigSideBar() {
  return (
    <Wrapper className="min-vh-100 d-flex flex-column align-items-center">BigSideBar</Wrapper>
  )
}

const Wrapper = styled.div`
  background-color: var(--box);
`
export default BigSideBar